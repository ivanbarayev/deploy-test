"use client";

// import { useTRPC } from "~/trpc/react";

export default function ProfilePage() {
  // const trpc = useTRPC();
  // const homepageListQuery = useQuery(trpc.homepage.list.queryOptions());
  // return <p>Profile page works</p>;
  return <></>;
}
